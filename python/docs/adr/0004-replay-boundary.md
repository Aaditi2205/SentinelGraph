# ADR 0004: test-derived replay, live decision services

Status: accepted.

The incident sequence is a deterministic fixture selected from locked test rows. Model inference, calibration, graph masking, cost policy, guardrail, webhook verification, analyst feedback, and audit chaining execute on request. The UI labels this boundary explicitly instead of calling the whole animation real-time ingestion.
