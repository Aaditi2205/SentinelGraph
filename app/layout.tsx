import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'SentinelGraph — Stop the ring, spare the customer',
  description:
    'A defense-only AI risk operations system that detects connected payment abuse and compiles bounded, auditable containment plans.',
  applicationName: 'SentinelGraph',
  keywords: ['fraud defense', 'payment risk', 'temporal graph', 'Razorpay', 'AI risk manager'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
